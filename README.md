# LDAP-Auth-ConsoleApp
This is a basic console application that connects to ActiveDirectory on WindowsServer through LDAP.
User can authenticate themselves if their user exists in ActiveDirectory server.
Thats it:)
